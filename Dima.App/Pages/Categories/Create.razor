@page "/categories/create"

<MudText Class="pt-8 pb-4" Typo="Typo.h3">Criar nova categoria</MudText>

<MudPaper Class="pa-4">
    <EditForm Model="@Category" FormName="CreateCategory" OnValidSubmit="CreateCategory">
        
        <DataAnnotationsValidator/>
        
        <MudTextField
            T="string"
            Label="Title"
            Placeholder="Digite a Título da nova categoria"
            @bind-Value="@Category.Title"
            InputType="InputType.Text"
            Required="true"
            Class="mb-6"/>

        <MudTextField
            T="string"
            Label="Description"
            Placeholder="Digite a descrição da nova categoria"
            @bind-Value="@Category.Description"
            InputType="InputType.Text"/>
        
        <div class="mt-6">
            @if (IsLoading)
            {
                <MudProgressCircular Indeterminate="true" Color="Color.Info"/>
            }
            else
            {
                <MudButton Variant="Variant.Filled" Color="Color.Primary" ButtonType="ButtonType.Submit">Criar</MudButton>
                <MudButton Href="/categories">Voltar</MudButton>
            }
        </div>
        
    </EditForm>
</MudPaper>


